---
name: product-architecture-teardown
description: 基于可复核产品证据拆解 AI 或软件产品，按用户层、技术层、模型层、数据层输出分级 HTML 架构报告。适用于产品逆向、竞品研究和架构审阅；不用于无证据猜测。
metadata:
  short-description: 证据驱动的产品架构拆解
---

# 产品架构拆解

将产品的可见行为、资产、状态和公开资料还原为一份可复核的产品拆解报告。目标不是猜内部实现，而是让读者知道：用户如何完成任务、哪些能力确实存在、哪些架构是行为推断，以及为了稳定实现这些能力还应补什么。

## 开始前：识别对象与证据门

先用一两句话确认要拆的产品和拆解目标。根据请求和证据，将产品暂分为下列一种或多种类型，并据此调整重心：

- **AIGC 创作工具**：重点看创作输入、资产链路、模型选择、生成任务、验收、编辑/导出、风格与一致性。
- **通用执行 Agent**：重点看目标分解、工具权限、计划、运行状态、审批、失败恢复、交接与可审计性。
- **对话陪伴或关系型产品**：重点看对话体验、记忆边界、人设/关系演进、主动性、安全与隐私，不把聊天文案误读为长期记忆事实。
- **通用软件或工作流产品**：重点看用户任务、信息架构、领域对象、权限、状态、集成和运营闭环。

在开始任何结论前建立证据清单。必须至少有一种可复核的一手产品证据，并确认它足以覆盖用户要分析的任务范围：

1. 覆盖关键流程的完整截图或录屏帧；
2. 可在当前会话、合法登录态下只读访问的产品网址/工作区；
3. 原始代码、可运行项目、接口定义或产品内部文档；
4. 产品公开的官方文档或官方页面（仅作公开能力的补充证据）。

没有任何一手证据时，**不要开始产品拆解**。说明缺少什么，并索要网址、关键流程截图或代码。证据零散时，先输出“可做范围 + 缺口清单”，只分析已有证据覆盖的范围；不能用宣传页、常识、相似产品或第三方文章填空。

需要浏览器补证时，只读操作：可查看公开页面、聊天、表单、画布、资产、历史记录、模型选项、状态与错误。除非用户已授权，禁止发送消息、生成、重试、发布、删除、购买、充值、覆盖资产或修改项目。不得读取或输出 Cookie、Token、密码、鉴权头、浏览器存储或个人敏感信息。遇到登录、验证码或需要用户接管时，把浏览器交给用户并等待。

对代码证据，优先用 `rg` 定位入口、状态、模型配置和实体定义；不执行会影响环境、数据或外部服务的命令。引用代码时说明文件和行号。

## 证据分级

每一个重要结论必须带且只带一种等级：

- **【已确认】**：页面、代码、官方资料或可复核运行结果直接支持。
- **【合理推断】**：由至少一条可见行为得出的最小解释；写出推导依据与限制。
- **【建议设计】**：为可靠性、体验、治理或可维护性提出的目标架构；不可描述成产品现状。
- **【未知】**：当前证据不足。把需要补看的页面、日志、代码或官方资料写清楚。

特别遵守：

- Agent 的公开计划、思考摘要或“已完成”文案，只是可观察输出，不是隐藏思维链、System Prompt 或已执行工具调用。
- 工具调用只有在结果、状态、调用记录或代码支持时才能确认；没有官方工具名时用“功能性名称（非官方）”。
- 一个按钮、入口或高亮选项不等于用户点击、参数提交、实际生成或计费成功。
- 画布、聊天、资产库和任务面板出现冲突时，记录并列事实，不能任选一处当真相。
- 后端语言、数据库、队列、云厂商、模型供应商、鉴权和支付实现没有证据时一律为未知；如有必要只能作为建议架构。

## 分层拆解流程

按以下顺序推进。产品类型决定每层的深度和子项，不要求每个产品都存在 Agent、模型或资产生成。

### 1. 用户层

还原真实用户任务和决策，而不是功能清单。记录：

- 用户目标、输入、上传素材、选择、确认、修改、中断与恢复；
- 页面反馈、按钮/表单、错误、等待与不确定性；
- 从进入产品到最终结果的主流程、修改流程、失败/中断流程；
- 账号、权限、余额、项目、历史、分享或发布等仅在有证据时记录；
- 用户是否真的看到了最终结果或只看到了“已完成”的口头说明。

对 AIGC 工具，额外检查剧本/提示词、风格、角色/场景/道具、分镜、生成、预览、剪辑和资产复用。对执行 Agent，额外检查审批、执行权限、外部工具副作用和交接。对陪伴产品，额外检查记忆告知、关系边界、危机/安全提示和用户控制权。

### 2. 技术层

从可见能力出发，拆解工作台、领域服务、Agent/工作流、工具和基础设施，不预设微服务或具体技术栈：

- 交互工作台：聊天、画布、卡片、表单、任务、预览、历史、错误；
- 应用能力：项目、内容/任务、资产、版本、发布、通知、权限；
- 编排能力：Agent 身份、触发条件、确认门、任务状态、交接、取消、重试、完成条件；
- 工具能力：读取/写入上下文、解析、检索、生成、执行、校验、导出；
- 运行能力：异步任务、对象存储、数据库、队列、日志、追踪、内容安全、计费。

将“当前可见架构”和“为实现这些能力所建议的架构”分成两张表。建议架构须解释解决哪一个已见风险或用户阻力。

### 3. 模型层

仅当产品使用或声称使用 AI/模型能力时拆解。本层要区分用户可见选项、真实调用证据与建议网关：

- 文本/Agent、图像、视频、音频、语音、检索/嵌入等模型类别；
- 模型选择、模式、分辨率、时长、参考素材、成本、延迟、错误或安全限制；
- Agent 如何获得模型能力知识，模型规则是否由工具/网关强制；
- 模型结果如何验收，用户反馈是否只影响当前项目还是可经授权进入评测数据；
- 未见模型路由时，不得因为页面出现一个模型就假定有动态路由或多个供应商。

建议架构中，模型能力、额度、安全和版权应由受控网关/服务执行，不能只依赖 Agent Prompt。

### 4. 数据层

将“全局上下文”拆为有边界的对象与版本，而非一个大框。按产品类型选用并标注为模板字段：

- UserContext：用户、语言、权限、可见权益、私有资产；
- ProjectConfig：任务配置、偏好、模型/规格的候选值和已确认值；
- DomainContext：剧本、文档、角色、场景、道具、计划、会话记忆或其他核心对象；
- AssetContext：图片、视频、音频、文件、版本、来源、访问范围；
- WorkflowState：当前阶段、AgentRun、Task、确认、错误、取消、重试和交接；
- BillingContext：预估、冻结、扣减、退款与账本；
- EvaluationContext：用户反馈、质量检查、失败原因和对账结果；
- KnowledgeContext：公共知识、模型能力、风格/模板、规则，以及用户/项目私有数据的隔离。

对每个对象说明生产者、消费者、读取/写入时机、稳定引用、版本、上游修改后的失效范围与权限边界。页面未显示真实字段名时使用语义化占位名，并标明“推导设计”。

## 交叉验证与风险审查

完成四层后，沿一条代表性任务把五条流同时串起来：用户交互、控制/编排、工具调用、上下文、资产。每步写明触发者、读取内容、判断、工具、结果、状态写入、确认门、下游和失败分支。

对 AIGC 或执行 Agent 至少审查：

- 聊天/画布/任务/资产状态是否一致；
- Agent 口头完成、工具结果、资产存在和用户验收是否被区分；
- 修改上游内容是否会影响下游；
- 生成重试是否幂等，是否可能重复扣费或产生重复资产；
- 余额、安全、权限、中断、回退和错误是否有可解释的状态；
- 用户私有资产、公共知识、项目临时数据是否隔离；
- 最终结果是否真的可预览/导出，而不只是出现入口。

没有失败样本时，写“未知”，再提出作为建议的安全恢复方案；不要凭空宣称产品缺少该能力。

## HTML 交付

最终统一产出一份可独立阅读的 HTML 文件。除非用户要求其他形式，不只在聊天中给长文本。HTML 应包含清晰锚点、证据标签、可横向阅读的表格，以及适合静态阅读的 Mermaid 代码块或图表。

按如下顺序组织，按产品类型删去不适用的空白部分，并明确“不适用”而不是硬补：

1. 执行摘要：一句话说明产品核心架构和证据边界；
2. 证据来源、查看范围与缺口；
3. 产品类型判断与拆解重心；
4. 用户层：功能域、用户旅程、主/修改/失败路径；
5. 技术层：端到端功能流转、分层架构、Agent/工具/服务协作；
6. 模型层：模型接入、选择/路由、能力限制、结果验证；
7. 数据层：上下文、知识/公共资产、实体表、关系图；
8. 代表性端到端时序图；
9. 全景架构主图，图例明确实线【已确认】、虚线【合理推断】、点线或特殊色【建议设计】；
10. As-Is 与 To-Be 对照；
11. 风险优先级；
12. 架构组件—证据追溯表：组件、必要性、证据、等级、置信度；
13. 仍然无法确认的问题及下一步只读取证方案。

在图中让普通产品经理能回答：需求从哪里进入、谁决定下一步、输入从哪里读取、工具/模型做什么、资产在哪里出现、用户如何确认/修改、失败/余额不足/中断如何处理、何时才算真正完成。图中不能包含没有证据且没有明确设计理由的组件。

交付前检查：HTML 内所有重要结论都有等级；证据编号可回溯；事实、推断、建议没有混写；不含秘密；没有把未点击入口当作已发生；HTML 的章节、锚点、表格和图代码结构完整。完成报告后停止，不要继续执行产品操作或扩展到未经请求的分析。
